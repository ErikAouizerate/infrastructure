---
all:
  hosts:
    app:
      ansible_host: 2.28.26.25
      # Final state: admin user on port 3254.
      # For a fresh reapply, switch to root on port 3254 first (cloud-init
      # already moved sshd there), run the playbook, then revert to admin.
      ansible_user: admin
      ansible_port: 3254
